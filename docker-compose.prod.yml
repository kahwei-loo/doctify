# ===========================================
# Doctify Docker Compose (Production)
# ===========================================
# Production environment with security hardening
#
# Security Features:
# - Traefik reverse proxy with Let's Encrypt SSL
# - Docker secrets for sensitive credentials
# - Non-root containers where possible
# - Network isolation (internal network for databases)
# - Resource limits (CPU/memory)
# - Read-only root filesystem where possible
# - Health checks for all services
#
# Usage:
#   docker-compose -f docker-compose.prod.yml up -d
#
# Prerequisites:
#   1. Create Docker secrets (see below)
#   2. Configure .env.prod file
#   3. Ensure domain DNS is configured

version: '3.8'

# ===========================================
# Docker Secrets (More secure than env vars)
# ===========================================
# Create secrets before running:
#   echo "your_password" | docker secret create postgres_password -
#   echo "your_password" | docker secret create redis_password -
#   echo "your_secret" | docker secret create secret_key -
#   echo "your_api_key" | docker secret create openai_api_key -
secrets:
  postgres_password:
    external: true
  redis_password:
    external: true
  secret_key:
    external: true
  openai_api_key:
    external: true

services:
  # ============================================================================
  # Traefik - Reverse Proxy & SSL Termination
  # ============================================================================
  traefik:
    image: traefik:v3.0
    container_name: doctify-traefik
    restart: always
    security_opt:
      - no-new-privileges:true
    ports:
      - "80:80"
      - "443:443"
    environment:
      - ACME_EMAIL=${ACME_EMAIL}
      - TRAEFIK_DASHBOARD_USER=${TRAEFIK_DASHBOARD_USER}
    volumes:
      - /var/run/docker.sock:ro
      - ./infrastructure/docker/traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./infrastructure/docker/traefik/dynamic:/etc/traefik/dynamic:ro
      - traefik_letsencrypt:/letsencrypt
      - traefik_logs:/var/log/traefik
    networks:
      - doctify-public
      - doctify-internal
    labels:
      # Dashboard
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard.rule=Host(`traefik.${DOMAIN}`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard`))"
      - "traefik.http.routers.dashboard.service=api@internal"
      - "traefik.http.routers.dashboard.middlewares=dashboard-auth@file"
      - "traefik.http.routers.dashboard.tls.certresolver=letsencrypt"
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M

  # ============================================================================
  # PostgreSQL Database (with pgvector)
  # ============================================================================
  doctify-postgres:
    image: pgvector/pgvector:pg16
    container_name: doctify-postgres-prod
    restart: always
    user: "999:999"  # postgres user
    environment:
      - POSTGRES_USER=${POSTGRES_USER:-doctify}
      - POSTGRES_PASSWORD_FILE=/run/secrets/postgres_password
      - POSTGRES_DB=${POSTGRES_DB:-doctify_production}
      - PGDATA=/var/lib/postgresql/data/pgdata
    secrets:
      - postgres_password
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infrastructure/docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - doctify-internal  # Internal only - not exposed to public
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-doctify} -d ${POSTGRES_DB:-doctify_production}"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  # ============================================================================
  # Redis Cache and Message Queue
  # ============================================================================
  doctify-redis:
    image: redis:7-alpine
    container_name: doctify-redis-prod
    restart: always
    user: "999:999"  # redis user
    command: >
      sh -c "redis-server
      --appendonly yes
      --requirepass $$(cat /run/secrets/redis_password)
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru"
    secrets:
      - redis_password
    volumes:
      - redis_data:/data
    networks:
      - doctify-internal  # Internal only
    healthcheck:
      test: ["CMD", "redis-cli", "--no-auth-warning", "-a", "$$(cat /run/secrets/redis_password)", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.1'
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

  # ============================================================================
  # Backend API Service
  # ============================================================================
  doctify-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        - BUILD_ENV=production
    image: doctify-backend:${VERSION:-latest}
    container_name: doctify-backend-prod
    restart: always
    user: "1000:1000"  # Non-root user
    read_only: true
    security_opt:
      - no-new-privileges:true
    environment:
      - ENVIRONMENT=production
      - DEBUG=false
      - LOG_LEVEL=WARNING
      - LOG_FORMAT=json
      # PostgreSQL
      - POSTGRES_HOST=doctify-postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USER=${POSTGRES_USER:-doctify}
      - POSTGRES_DB=${POSTGRES_DB:-doctify_production}
      # Redis (password from secret file)
      - REDIS_HOST=doctify-redis
      - REDIS_PORT=6379
      # Celery
      - CELERY_BROKER_URL=redis://doctify-redis:6379/1
      - CELERY_RESULT_BACKEND=redis://doctify-redis:6379/1
      # CORS
      - BACKEND_CORS_ORIGINS=["https://${DOMAIN}","https://www.${DOMAIN}"]
      # Metrics
      - ENABLE_PROMETHEUS=true
    secrets:
      - postgres_password
      - redis_password
      - secret_key
      - openai_api_key
    volumes:
      - backend_uploads:/app/uploads
      - backend_logs:/app/logs
      - backend_tmp:/tmp  # Required for read_only
    tmpfs:
      - /app/.cache:mode=1777,size=100M
    depends_on:
      doctify-postgres:
        condition: service_healthy
      doctify-redis:
        condition: service_healthy
    networks:
      - doctify-internal
    labels:
      - "traefik.enable=true"
      # API Router
      - "traefik.http.routers.api.rule=Host(`api.${DOMAIN}`)"
      - "traefik.http.routers.api.entrypoints=websecure"
      - "traefik.http.routers.api.tls.certresolver=letsencrypt"
      - "traefik.http.routers.api.middlewares=security-headers@file,api-rate-limit@file,compress@file"
      - "traefik.http.services.api.loadbalancer.server.port=8000"
      # Health check endpoint
      - "traefik.http.routers.api-health.rule=Host(`api.${DOMAIN}`) && Path(`/health`)"
      - "traefik.http.routers.api-health.entrypoints=websecure"
      - "traefik.http.routers.api-health.tls.certresolver=letsencrypt"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  # ============================================================================
  # Celery Worker (Background Tasks)
  # ============================================================================
  doctify-celery:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        - BUILD_ENV=production
    image: doctify-backend:${VERSION:-latest}
    container_name: doctify-celery-prod
    restart: always
    user: "1000:1000"
    read_only: true
    security_opt:
      - no-new-privileges:true
    command: celery -A app.tasks.celery_app worker -l warning --pool=prefork --concurrency=2 -Q ocr_queue,default
    environment:
      - ENVIRONMENT=production
      - DEBUG=false
      - LOG_LEVEL=WARNING
      - LOG_FORMAT=json
      # PostgreSQL
      - POSTGRES_HOST=doctify-postgres
      - POSTGRES_PORT=5432
      - POSTGRES_USER=${POSTGRES_USER:-doctify}
      - POSTGRES_DB=${POSTGRES_DB:-doctify_production}
      # Redis
      - REDIS_HOST=doctify-redis
      - REDIS_PORT=6379
      # Celery
      - CELERY_BROKER_URL=redis://doctify-redis:6379/1
      - CELERY_RESULT_BACKEND=redis://doctify-redis:6379/1
    secrets:
      - postgres_password
      - redis_password
      - secret_key
      - openai_api_key
    volumes:
      - backend_uploads:/app/uploads
      - backend_logs:/app/logs
      - backend_tmp:/tmp
    tmpfs:
      - /app/.cache:mode=1777,size=100M
    depends_on:
      - doctify-postgres
      - doctify-redis
      - doctify-backend
    networks:
      - doctify-internal
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '0.25'
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

  # ============================================================================
  # Celery Beat (Task Scheduler)
  # ============================================================================
  doctify-celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
      args:
        - BUILD_ENV=production
    image: doctify-backend:${VERSION:-latest}
    container_name: doctify-celery-beat-prod
    restart: always
    user: "1000:1000"
    read_only: true
    security_opt:
      - no-new-privileges:true
    command: celery -A app.tasks.celery_app beat -l warning --scheduler celery.beat:PersistentScheduler
    environment:
      - ENVIRONMENT=production
      - DEBUG=false
      - LOG_LEVEL=WARNING
      # Redis
      - REDIS_HOST=doctify-redis
      - REDIS_PORT=6379
      # Celery
      - CELERY_BROKER_URL=redis://doctify-redis:6379/1
      - CELERY_RESULT_BACKEND=redis://doctify-redis:6379/1
    secrets:
      - redis_password
    volumes:
      - celery_beat_data:/app/celerybeat-schedule
    tmpfs:
      - /tmp:mode=1777,size=10M
    depends_on:
      - doctify-redis
    networks:
      - doctify-internal
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
        reservations:
          cpus: '0.05'
          memory: 32M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # ============================================================================
  # Prometheus (Metrics Collection)
  # ============================================================================
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: doctify-prometheus-prod
    restart: always
    user: "65534:65534"  # nobody user
    security_opt:
      - no-new-privileges:true
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--storage.tsdb.retention.time=30d'
      - '--web.enable-lifecycle'
    volumes:
      - ./infrastructure/docker/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./infrastructure/docker/prometheus/alerts:/etc/prometheus/alerts:ro
      - prometheus_data:/prometheus
    networks:
      - doctify-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.prometheus.rule=Host(`prometheus.${DOMAIN}`)"
      - "traefik.http.routers.prometheus.entrypoints=websecure"
      - "traefik.http.routers.prometheus.tls.certresolver=letsencrypt"
      - "traefik.http.routers.prometheus.middlewares=admin-whitelist@file,dashboard-auth@file"
      - "traefik.http.services.prometheus.loadbalancer.server.port=9090"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          cpus: '0.1'
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

  # ============================================================================
  # Grafana (Monitoring Dashboard)
  # ============================================================================
  grafana:
    image: grafana/grafana:10.2.2
    container_name: doctify-grafana-prod
    restart: always
    user: "472:472"  # grafana user
    security_opt:
      - no-new-privileges:true
    environment:
      - GF_SECURITY_ADMIN_PASSWORD__FILE=/run/secrets/grafana_password
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=https://grafana.${DOMAIN}
      - GF_ALERTING_ENABLED=true
      - GF_UNIFIED_ALERTING_ENABLED=true
      - GF_SECURITY_COOKIE_SECURE=true
      - GF_SECURITY_STRICT_TRANSPORT_SECURITY=true
    volumes:
      - grafana_data:/var/lib/grafana
      - ./infrastructure/docker/grafana/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./infrastructure/docker/grafana/datasources:/etc/grafana/provisioning/datasources:ro
    networks:
      - doctify-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.grafana.rule=Host(`grafana.${DOMAIN}`)"
      - "traefik.http.routers.grafana.entrypoints=websecure"
      - "traefik.http.routers.grafana.tls.certresolver=letsencrypt"
      - "traefik.http.services.grafana.loadbalancer.server.port=3000"
    depends_on:
      - prometheus
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M
    logging:
      driver: "json-file"
      options:
        max-size: "20m"
        max-file: "3"

  # ============================================================================
  # AlertManager (Notifications)
  # ============================================================================
  alertmanager:
    image: prom/alertmanager:v0.26.0
    container_name: doctify-alertmanager-prod
    restart: always
    user: "65534:65534"
    security_opt:
      - no-new-privileges:true
    command:
      - '--config.file=/etc/alertmanager/alertmanager.yml'
      - '--storage.path=/alertmanager'
      - '--web.external-url=https://alertmanager.${DOMAIN}'
    volumes:
      - ./infrastructure/docker/alertmanager/alertmanager.yml:/etc/alertmanager/alertmanager.yml:ro
      - alertmanager_data:/alertmanager
    networks:
      - doctify-internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.alertmanager.rule=Host(`alertmanager.${DOMAIN}`)"
      - "traefik.http.routers.alertmanager.entrypoints=websecure"
      - "traefik.http.routers.alertmanager.tls.certresolver=letsencrypt"
      - "traefik.http.routers.alertmanager.middlewares=admin-whitelist@file,dashboard-auth@file"
      - "traefik.http.services.alertmanager.loadbalancer.server.port=9093"
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
        reservations:
          cpus: '0.05'
          memory: 32M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

# ===========================================
# Volumes for Data Persistence
# ===========================================
volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local
  backend_uploads:
    driver: local
  backend_logs:
    driver: local
  backend_tmp:
    driver: local
  celery_beat_data:
    driver: local
  prometheus_data:
    driver: local
  grafana_data:
    driver: local
  alertmanager_data:
    driver: local
  traefik_letsencrypt:
    driver: local
  traefik_logs:
    driver: local

# ===========================================
# Network Configuration
# ===========================================
networks:
  # Public network - for Traefik and services that need internet access
  doctify-public:
    driver: bridge
    name: doctify-public

  # Internal network - for database and internal services
  # Services on this network cannot be reached from outside
  doctify-internal:
    driver: bridge
    name: doctify-internal
    internal: true  # No external connectivity
